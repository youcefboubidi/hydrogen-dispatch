# Phase 0 environment (PROJECT_PLAN.md section 6), restricted to what Phases 0-7 need.
# Pinned to the versions the Phase 2 ETAP validation was run with (Python 3.14.3).
pandapower==3.4.0
numpy==2.3.5
pandas==2.3.3
jupyter==1.1.1
matplotlib==3.11.0  # network diagrams in notebooks/validation.ipynb + dashboard power-balance chart
scipy==1.16.3  # Phase 3: electrolyzer i(P) inversion (brentq)
pymoo==0.6.1.6  # Phase 5: PSO dispatch optimizer
streamlit==1.58.0  # Phase 7: supervisory dashboard (app/dashboard.py)
